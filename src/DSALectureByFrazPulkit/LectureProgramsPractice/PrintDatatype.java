package DSALectureByFrazPulkit.LectureProgramsPractice;
import java.util.Scanner;
import java.lang.*;

public class PrintDatatype {
	public static void main(String[] arg)
	{
		// Declare a variable with a custom type "val"
        int val = 42;
/*
        // Check the type using instanceof
        if (val instanceof Integer) {
            System.out.println("val is of type Integer");
        } else if (val instanceof Double) {
            System.out.println("val is of type Double");
        } else if (val instanceof String) {
            System.out.println("val is of type String");
        } else {
            System.out.println("val is of unknown type");
        }
        */
	}
}
